# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently a bare scaffold: it contains only `README.md` (a single-line title, "# 3DCORE"). There is no source code, package manifest, build system, linter, or test suite yet.

Because there is nothing to build, lint, test, or run, this file has no commands or architecture notes to give. When real code is added, update this file with:

- The actual build/lint/test commands (and how to run a single test), once a package manager or build system is chosen.
- The high-level architecture and module boundaries, once the codebase has enough structure for that to matter.
